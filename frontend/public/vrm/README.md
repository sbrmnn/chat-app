# VRM models

The actual `.vrm` files are not committed (each ~20-30MB). Fetch them with:

```bash
bin/setup-assets
```

Expected files:
- `saki.vrm`
- `yuki.vrm`
- `hana.vrm`
- `aoi.vrm`
- `koharu.vrm`
- `mei.vrm`
